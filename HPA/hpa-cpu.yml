apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy
  labels:
    name: nginx-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-app-pod
  template:
    metadata:
      labels:
        app: nginx-app-pod
    spec:
      containers:
      - name: nginx-container
        image: nginx:latest
        resources:
          limits:
            cpu: "20m"
        ports:
        - containerPort: 80
---

apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx-app-pod
  ports:
  - port: 80
    targetPort: 80
---

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: nginx-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nginx-app-pod
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 10

